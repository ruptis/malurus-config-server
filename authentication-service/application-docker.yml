server:
  port: 8089

spring:
  datasource:
    url: jdbc:postgresql://postgres-authentication-service:5432/authentication-service

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka

management:
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
